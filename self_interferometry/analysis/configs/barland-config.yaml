model:
  # Common parameters for all model types
  role: "standard" # "standard" or "ensemble"
  type: "Barland"
  input_size: 256
  output_size: 1
  activation: "LeakyReLU"
  in_channels: 3
  dropout: 0.1
  # BarlandCNN specific parameters
  window_stride: 128

training:
  max_epochs: 500
  warmup_epochs: 0
  batch_size: 256
  optimizer: "Adam"  # Options: "Adam", "SGD", "Muon"
  momentum: 0.9 # Unused if optimizer is not SGD
  learning_rate: 7e-2
  eta_min: 0
  accelerator: "gpu"
  devices: 1
  use_logging: true
  wandb_project: "self-interferometry"
  experiment_name: "barland_cnn_training"
  checkpoint_dir: "./analysis/checkpoints"
  target: "velocity"  # "velocity" or "displacement" - what the model predicts

loss:
  dynamic: true # enable dynamic loss weighting, overrides weights below
  velocity_loss_weight: 1.0
  displacement_loss_weight: 0.1

data:
  data_dir: "./analysis/data"
  dataset_file: "dataset-mmfiber.h5"
  split_ratios: [80, 10, 10]  # train, val, test percentages
  split_seed: 42
  num_workers: 7
  channel_dropout: 0.5  # chance of dropping a channel during training
